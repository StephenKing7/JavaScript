var isPalindrome = function(x) {
    forwards = String(x);

    backwards = String(x).split("").reverse().join("");

    if (forwards === backwards) {
        return true;
    }

    return false;

};

isPalindrome();


/*
PROMPT:

Given an integer x, return true if x is a palindrome, and false otherwise.

*/
